// Config for this route
export const dynamic = 'force-dynamic'; // Opt out of static site generation
export const dynamicParams = true; // All params are considered dynamic 
export const revalidate = 0; // No cache, always revalidate 